# Start n8n
docker compose up -d

# Start ngrok if enabled in config.json
$config = Get-Content -Path "$PSScriptRoot/../config.json" | ConvertFrom-Json
if ($config.ngrok.enabled) {
    ngrok http 5678
}

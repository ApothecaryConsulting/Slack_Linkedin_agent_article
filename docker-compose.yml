services:
  n8n:
    build: .
    ports:
      - "5678:5678"
    volumes:
      - ./n8n_data:/home/node/.n8n
      - ./n8n_data/.n8n-files/capability_statements:/home/node/.n8n-files/capability_statements
    env_file:
      - .env
    restart: unless-stopped
